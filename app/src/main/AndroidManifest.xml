<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.addstudentdetailsproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="activity.Update_Delete"></activity>
        <activity android:name="activity.RegistrationPageActivity" />
        <activity android:name="activity.ShowAllStudent" />
        <activity android:name="activity.login.LoginPageActivity" />
        <activity android:name="activity.login.LoginRegisterActivity" />


        <activity android:name="activity.AddorShowAllActivity" />
        <activity android:name="activity.SplashScreenlogoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>